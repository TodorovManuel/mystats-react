import React from "react";
import NavBar from "../NavBar/NavBar";
const Agregar = () => {
    return (
        <div>
            <NavBar />
            agregar
        </div>
    );
}

export default Agregar;